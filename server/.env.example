# TaskPulse Server Environment Configuration
# Copy this file to .env and update with your actual values

# ============================================
# SERVER CONFIGURATION
# ============================================

# Server port number
# The port on which the TaskPulse API server will listen
# Default: 3000
PORT=3000

# Environment mode
# Set to 'production' for production deployments
# Set to 'development' for development (shows detailed error messages)
# Default: development
NODE_ENV=development

# ============================================
# SECURITY CONFIGURATION
# ============================================

# Cookie secret for session encryption
# IMPORTANT: Change this to a random, secure string in production!
# Used to encrypt session cookies for user authentication
# Generate a secure random string using: openssl rand -base64 32
# Default: change-this-to-a-random-string-in-production (NOT SECURE FOR PRODUCTION!)
COOKIE_SECRET=change-this-to-a-random-string-in-production

# ============================================
# API ACCESS
# ============================================

# Bot API Token (for CLI usage)
# This is added automatically when you create a bot via setup.js or user-cli.js
# Format: bot_{random_string}
# Used by the CLI (cli-api.js) to authenticate as a bot
# Example: TASKPULSE_API_TOKEN="bot_abc123def456ghi789"
# TASKPULSE_API_TOKEN=""

# ============================================
# DEVELOPMENT & DEBUGGING
# ============================================

# Enable debug mode
# Set to '1' or 'true' to enable detailed debug output
# Shows stack traces and verbose logging
# Default: (not set)
# DEBUG=1

# ============================================
# INTERNAL USE
# ============================================

# CLI Mode (internal - do not set manually)
# Set automatically by CLI scripts (cli.js, cli-api.js)
# Suppresses certain database output for cleaner CLI experience
# Default: (not set, set by scripts)
# CLI_MODE=1