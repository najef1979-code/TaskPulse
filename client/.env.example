# TaskPulse Client Configuration
# Copy this file to .env and update with your actual values

# ============================================
# NETWORK CONFIGURATION
# ============================================

# External domain where the app is deployed (public/internet access)
# The app uses this to detect if it's being accessed from outside your network
# Example: taskpulse.ceraimic.eu or task.yourdomain.com
VITE_EXTERNAL_DOMAIN=yourdomain.com

# Local IP address for internal network access
# Used when accessing the app from devices on the same network (WiFi, LAN)
# Example: 192.168.1.100, 10.0.0.5, or localhost for local development
VITE_LOCAL_IP=192.168.1.100

# ============================================
# OPTIONAL OVERRIDES
# ============================================

# Manual API URL override (optional)
# If set, this takes precedence over the auto-detection logic above
# Leave commented out to use smart environment detection
# Uncomment and set if you need to force a specific API endpoint
# Examples:
#   - For local development: VITE_API_URL=http://localhost:3000/api
#   - For staging: VITE_API_URL=https://staging-api.yourdomain.com
# VITE_API_URL=http://localhost:3000/api

# ============================================
# APPLICATION SETTINGS
# ============================================

# Allow new user registration
# Set to 'true' to enable the registration link on the login page
# Set to 'false' to hide registration (only admin can create user accounts)
# Security: Use 'false' for internal/private applications
# Public: Use 'true' for public-facing applications
VITE_ALLOW_REGISTRATION=true
